"use client";
import { useEffect,useState } from "react";import { useParams,useRouter } from "next/navigation";import Link from "next/link";import MainLayout from "../../componentes/MainLayout";import { BadgeCheck,Building2,Clock3,MapPin,MessageCircle,Phone,ShieldCheck,Star } from "lucide-react";import "../../styles/servicios.css";
const API="https://new-backend-lovat.vercel.app/api";
export default function ServiceDetail(){const {id}=useParams();const router=useRouter();const [data,setData]=useState<any>(null);useEffect(()=>{fetch(`${API}/services/${id}`,{cache:"no-store"}).then(r=>r.json()).then(setData)},[id]);if(!data?.profile)return <MainLayout><div className="services-empty">Cargando perfil…</div></MainLayout>;const p=data.profile;const chat=async()=>{const token=localStorage.getItem("marketplace_token");if(!token){router.push(`/login?next=/servicios/${id}`);return}const r=await fetch(`${API}/chat/start`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify({participantId:p.owner?._id})});const c=await r.json();if(r.ok)router.push(`/chatpage?conversationId=${c._id}`)};const wa=String(p.whatsapp||p.phone||"").replace(/\D/g,"");return <MainLayout><div className="service-detail"><section className="profile-panel"><img className="profile-avatar" src={p.avatar||p.owner?.avatar||"/assets/offerton.jpg"} alt={p.displayName}/><div className="profile-main"><div className="provider-name"><h1>{p.displayName}</h1>{p.verificationStatus==="verified"&&<BadgeCheck/>}</div><p>{p.headline||p.trades.join(" · ")}</p><div className="trade-tags">{p.trades.map((x:string)=><span key={x}>{x}</span>)}</div><div className="profile-facts"><span><Star/> {p.rating?Number(p.rating).toFixed(1):"Perfil nuevo"}</span><span><Clock3/> {p.experienceYears} años de experiencia</span><span><MapPin/> {p.neighborhoods?.join(" · ")||"Rosario"}</span>{p.verificationStatus==="verified"&&<span><ShieldCheck/> Identidad y experiencia verificadas</span>}</div></div><aside className="contact-box">{p.availableNow&&<b className="available-inline">Disponible ahora</b>}<button onClick={()=>void chat()}><MessageCircle/> Escribir por chat</button>{wa&&p.contactPreference!=="chat"&&<a href={`https://wa.me/${wa.startsWith("54")?wa:`54${wa}`}?text=${encodeURIComponent(`Hola ${p.displayName}, te encontré en Rosario Market y necesito consultar por un servicio.`)}`} target="_blank"><Phone/> Consultar por WhatsApp</a>}{p.startingPrice!=null&&<small>Visita desde ${Number(p.startingPrice).toLocaleString("es-AR")}</small>}</aside></section><section className="profile-copy"><h2>Sobre su trabajo</h2><p>{p.bio||"Este profesional todavía no agregó una descripción detallada."}</p>{p.schedule&&<p><b>Horarios:</b> {p.schedule}</p>}{p.showAddress&&p.address&&<p><b>Dirección:</b> {p.address}</p>}</section>{data.recommendedBusinesses?.length>0&&<section className="recommended"><h2>Comercios relacionados con este oficio</h2><p>Solo mostramos ferreterías y negocios compatibles con el trabajo de este profesional.</p><div className="recommended-grid">{data.recommendedBusinesses.map((b:any)=><Link href={`/negocio/${b._id}`} key={b._id}><img src={b.logo||"/assets/navbarbolsa.png"} alt=""/><div><b>{b.name}</b><span>{b.categories?.join(" · ")}</span></div><Building2/></Link>)}</div></section>}</div></MainLayout>}
